spring:
  #  mvc:
  #    static-path-pattern: /res/**          #设置静态资源访问前缀

  datasource:
    url: jdbc:mysql:///mybatis?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.shiliuzi.personnel_management